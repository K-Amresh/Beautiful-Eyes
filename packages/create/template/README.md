# __PROJECT_NAME__

A [Beautiful Eyes](https://beautiful-eyes.netlify.app/) app.

```sh
npm start
```

Opens webpack-dev-server on http://localhost:9000. `src/app.ts` is the component class; `src/app.template.be` is the template.
